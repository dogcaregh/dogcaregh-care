export const COMMISSION_RATE = 0.25;

// Referral rewards: a provider earns this share of every paid booking
// made by an owner they referred, for REFERRAL_WINDOW_MONTHS after that
// owner signs up. Keep REFERRAL_RATE in sync with add_referrals.sql.
export const REFERRAL_RATE = 0.05;
export const REFERRAL_WINDOW_MONTHS = 12;
